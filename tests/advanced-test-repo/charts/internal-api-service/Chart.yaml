apiVersion: v2
name: internal-api-service
description: Internal API Service - LOW Exposure (Internal only)

type: application

version: 0.1.0
appVersion: "1.0.0"

annotations:
  reticulum.exposure: "LOW"
  reticulum.category: "internal-service"
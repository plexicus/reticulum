apiVersion: v2
name: external-api-service
description: External API Service - HIGH Exposure (Ingress enabled)

type: application

version: 0.1.0
appVersion: "1.0.0"

annotations:
  reticulum.exposure: "HIGH"
  reticulum.category: "external-service"